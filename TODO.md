## To Do:

### The project is transferring to Raspberry Pi 3 as the Pi Zero isn't powerful enough, and makes the build awkward with all the hats.

- Remake and redesign the UI for the new screen, the UI should include:
1) Radar map
2) Stats such as active planes, current coordinates, ADSB and GPS status
3) Add a dedicated "Selected Plane" section that shows information about the plane the user tapped on.

- add touch features as the project now contains a touchscreen. These will include:
1) Zoom in
2) Zoom out
3) Hide/Show Plane Icon Text
4) Exit

- Add support for GPS where the output of the module is the home coordinates, this will update every 10 seconds.


